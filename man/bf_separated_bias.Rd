% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bf_worst_case.R
\name{bf_separated_bias}
\alias{bf_separated_bias}
\title{Separated Bayes factor granting a search biased toward the working theory}
\usage{
bf_separated_bias(y_W, y_R, g, omega = 1)
}
\arguments{
\item{y_W, y_R}{Non-negative integers. Counts favoring the working
theory and the rival.}

\item{g}{Numeric strictly between 0 and 0.5. The margin separating the
two theories' claims from one half.}

\item{omega}{Positive numeric. The odds by which the search favored
surfacing evidence for the working theory. Default \code{1}, an unbiased
search.}
}
\value{
A length-1 numeric: the bias-corrected separated Bayes factor.
}
\description{
Suppose the researcher's search was \code{omega} times as likely to surface
a document supporting the working theory as one supporting the rival.
Then part of the apparent dominance of her counts comes from how she
looked rather than from what is there. The bias turns a share
\eqn{\theta} of supporting documents in the collection into a share
\eqn{\omega\theta / (\omega\theta + 1 - \theta)} among the documents
she found, and this function compares the two theories' claims after
both have been tilted that way. At \code{omega = 1} it returns
\code{\link[=bf_separated]{bf_separated()}} exactly.
}
\details{
Granting more bias lowers the value: conceding that the search favored
the working theory weakens what the counts can show.

The error-rate guarantee that \code{\link[=bf_worst_case]{bf_worst_case()}} carries has been
checked numerically for this statistic at every \code{omega} at or above
one that the authors tried, and it provably fails below one. A proof
for \code{omega >= 1} is still owed, so tables built from this function
should say that the guarantee was checked numerically rather than
proved.
}
\examples{
# An unbiased search recovers the uncorrected value.
bf_separated_bias(9, 3, 0.125)

# Granting that the search favored the working theory by half again
# takes the running example from about 21 down to about 6.4.
bf_separated_bias(9, 3, 0.125, omega = 1.5)

}
\seealso{
\code{\link[=bf_separated]{bf_separated()}} for the uncorrected version;
\code{\link[=sens_binomial]{sens_binomial()}} for sensitivity to \code{omega} under \code{\link[=bf_binomial]{bf_binomial()}}.
}
