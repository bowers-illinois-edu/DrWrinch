% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sens_urn.R
\name{sens_urn}
\alias{sens_urn}
\title{Sensitivity analysis for the urn Bayes factor}
\usage{
sens_urn(y_W, y_R, threshold = 20)
}
\arguments{
\item{y_W}{Non-negative integer. Observed count favorable to the
working theory.}

\item{y_R}{Non-negative integer. Observed count favorable to the
rival.}

\item{threshold}{Positive numeric. Decision threshold the Bayes
factor must remain at or above. Default \code{20}.}
}
\value{
A list with elements:
\describe{
\item{\code{bf}}{Bayes factor at \code{omega = 1}.}
\item{\code{omega_star}}{Bias tipping point. \code{0} if \code{bf < threshold}
at baseline; \code{NA_real_} if \code{bf} does not cross \code{threshold} for
any reachable \code{omega}.}
}
}
\description{
Reports the bias tipping point for the urn Bayes factor: the
smallest observation bias \code{omega > 1} at which the Bayes factor
first drops below \code{threshold}.
}
\details{
\code{omega > 1} makes pro-\eqn{H_1} items more likely to be drawn than
they are in the urn, so the apparent dominance of pro-\eqn{H_1}
evidence is partly an artifact of observation, and the Bayes factor
falls. \code{omega_star} is the value at which this fall first crosses
\code{threshold}. If \code{bf_urn(y_W, y_R) < threshold} at baseline,
\code{omega_star} is \code{0}.
}
\examples{
# The paper's running example. bf_urn(9, 3) = 323 is far above 20, so
# pro-H_1 items must be ~2.4 times likelier to be observed before the
# urn BF reaches the threshold.
s <- sens_urn(9, 3)
s$bf
s$omega_star

}
\seealso{
\code{\link[=bf_urn]{bf_urn()}}, \code{\link[=sens_binomial]{sens_binomial()}}, \code{\link[=sens_coding]{sens_coding()}}.
}
